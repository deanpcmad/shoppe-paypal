en:
  shoppe:
    settings:
      labels:
        paypal_client_id: Client ID
        paypal_client_secret: Secret ID
        paypal_currency: Currency Code
        paypal_mode: Paypal Mode

      types:
        paypal_mode: boolean

      options:
        paypal_mode:
          affirmative: Sandbox
          negative: Live

      help:
        paypal_client_id: These details can be found after creating a REST API Application - https://developer.paypal.com/developer/applications
        paypal_currency: The currency which should be sent to PayPal with each of your payment requests

      defaults:
        paypal_currency: USD
        paypal_mode: true
